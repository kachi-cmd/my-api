# My Personal API

A simple REST API built with Node.js and Express.

## Run locally
npm install
node index.js

## Endpoints
- GET /        → { "message": "API is running" }
- GET /health  → { "message": "healthy" }
- GET /me      → { name, email, github }

## Live URL
http://kchiii.duckdns.org
